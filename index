<!DOCTYPE html>
<html>
<head><title>Мои питомцы</title></head>
<body>
<h2>Здравствуйте, {{ user['username'] }}!</h2>
<a href="{{ url_for('logout') }}">Выйти</a>
<a href="{{ url_for('add_pet') }}">Добавить питомца</a>

<h3>Питомцы:</h3>
{% for pet in pets %}
<div style="border: 1px solid #ccc; padding: 10px; margin: 10px;">
<h4>{{ pet['name'] }} ({{ pet['species'] }})</h4>
<p>Возраст: {{ pet['age'] }}</p>
{% if pet['photo'] %}
<img src="{{ url_for('static', filename='uploads/' + pet['photo']) }}" width="150" alt="{{ pet['name'] }}">
{% endif %}
<p>Здоровье: {{ pet['health'] }}</p>
<p>Счастье: {{ pet['happiness'] }}</p>
<p>Голод: {{ pet['hunger'] }}</p>
<form method="post" action="{{ url_for('feed_pet', pet_id=pet['id']) }}">
<button type="submit">Кормить</button>
</form>
<form method="post" action="{{ url_for('play_with_pet', pet_id=pet['id']) }}">
<button type="submit">Играть</button>
</form>
<form method="post" action="{{ url_for('delete_pet', pet_id=pet['id']) }}">
<button type="submit">Удалить</button>
</form>
</div>
{% endfor %}
</body>
</html>
